import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#355fa4',
    },
    secondary: {
      main: '#e64e2a',
    },
    success: {
      main: '#4ba93f',
    },
  },
});

export default theme;